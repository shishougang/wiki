#+SETUPFILE: ~/.emacs.d/src/org-templates/level-2.org
#+TITLE: 浅谈设计模式八: 适配器模式(Adapter)
#+OPTIONS: num:nil H:2

* 实例

* 适配器模式(Adapter)
** 目的

** 实现

* 总结
** 适配器模式(Adapter)结构
1. 类适配器使用多重继承来使一个接口适配另外一个：
[[./images/adapter/adapter_structure1.jpg]]
2. 一个对象适配器依赖于对象聚合：
[[./images/adapter/adapter_structure2.jpg]]
** 组成
+ 目标对象（Target）
  定义客户（Client)使用的特定领域的接口。
+ 客户对象（Client）
  利用符合目标（Target）接口的对象协作。
+ 被适配对象（Adaptee）
  定义一个需要被适配的已经存在的接口。
+ 适配器（Adapter）
  适配被适配对象（Adaptee）接口到目标（Target）接口。
** 应用场景
+ 你想使用一个已经存在的类，但是它的接口不符合你的需求。
+ 你想创建一个可复用的类，并与不相关或不可预见的类

** 缺点
